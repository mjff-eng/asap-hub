import { colorFromHex, colour } from '@asap-hub/react-components';

const colors = {
  primary500: colorFromHex(colour.brand.gp2[500]),
  primary900: colorFromHex(colour.brand.gp2[800]),
};
export default colors;
